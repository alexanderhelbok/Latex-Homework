\documentclass{alex_summary}

\begin{document}
\section*{Reading summary}

In \blockcquote{divincenzo_physical_2000}{The Physical Implementation of Quantum Computation} David DiVincenzo describes a set of \textbf{5 criteria} a physical system must fulfill in order for it to function as a \textbf{quantum computer}, as well as \textbf{two additional} requirements for \textbf{quantum communication}. This is highly relevant because the framework of quantum information processing does not specify a physical system to implement its theoretical concepts. Therefore many platforms have been proposed as potential candidates for quantum computation, these criteria help evaluate the feasibility of different platforms and identify their shortcomings

\begin{enumerate}[leftmargin=*]
	\item \textit{A scalable physical system with well characterized qubits.} A system with \textbf{well defined states} is needed, from which two are picked to serve as ground state and exited state, denoted \( \ket{0} \) and \( \ket{1} \). Furthermore, under \blockquote{well characterized} also fall a lot of other things, like the \textbf{coupling to other qubits and the environment}. The system also needs to \textbf{allow scaling}, as quantum computation is only practical in multi-qubit setups.
	%
	\item \textit{The ability to initialize the state of the qubits to a simple fiducial state.} Once we have a set of functioning qubits, we need to reliably prepare them in a \textbf{predefined state} (usually the ground state). This is usually done by \textbf{cooling} the system and allowing the qubits decay into their ground state. Alternatively one can \textbf{perform measurements} on the qubits that collapse the wavefunction into a state with high overlap with a desired state.
	%
	\item \textit{Long relevant decoherence times, much longer than the gate operation time.} \textbf{Decoherence} measures how long quantum information is retained before it is \textbf{lost to the environment}. In order to \textbf{successfully retrieve} a result from a calculation consisting of multiple gate operation on qubits, information needs to be retained \textbf{(orders of magnitude) longer} than the actual calculation time. 
	%
	\item \textit{A “universal” set of quantum gates.} There are a lot of different logical operations that can be used on qubits but not all are physically implementable. What we therefore do is \textbf{decompose} a complicated operation into more simple ones. The most basic logic gates are the \textbf{quantum XOR} and \textbf{cNOT}. A fiducial implementation of these is sufficient for quantum computation, with more complex ones being useful in reducing gate operation times and error rates.
	%
	\item \textit{A qubit-specific measurement capability.} Finally, we want to \textbf{extract a result} from a calculation by directly measuring the state of the computation qubit. This measurement should leave \textbf{other qubits} in the system \textbf{untouched} (preserving their state). Ideally, we also want the measurement to be \textbf{non-destructive}, leaving the probed qubit in a known (instead of random) state, but this is not stringently required.
	%
	\item \textit{The ability to interconvert stationary and flying qubits.} If we want to build a \textbf{quantum network} we need a way to send quantum information back and forth between nodes. We therefore need to \textbf{transfer} the quantum state present on the local qubit to a flying qubit which carries the information to its destination, where it is then \textbf{imprinted} onto a local state. 	
	%
	\item \textit{The ability faithfully to transmit flying qubits between specified locations.} Beyond initiating the quantum communication, we also need a way to \textbf{transmit information} without significant \textbf{signal degradation}. \textbf{Photons and optical cables} seem the most convincing solution to date, but electronic states is metals have also been proposed.\\
\end{enumerate}

\begin{question1}
	This may be a more general question, but have there been any corrections, alternative formulations, or addenda to the DV criteria? If so, why have they not been widely adopted in the QIP field?
\end{question1}

\begin{question3}
	Can we work around criterion 3 if we use error correction codes? it would probably be rephrased to "..., much longer than error correction readout and mitigation"
\end{question3}

\printbibliography
\end{document}
